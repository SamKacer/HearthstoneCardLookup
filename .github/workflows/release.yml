name: publish release to NVDA aAddon store
on: push
  # release:
    # types:
      # - published
permissions:
  contents: read
  issues: write
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: post issue 
        run: |
          gh issue create --title hello2 --body "another hellow world"
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          # ASSETS: ${{ toJson(github.event.release.assets) }}
          # SOURCE_URL: https://github.com/${{ github.reposit/ory }}
          # RELEASE_VERSION: ${{ github.event.release.name }}